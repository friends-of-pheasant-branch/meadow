# Site settings
title:                Meadow
email:                your-email@domain.com
description:          > # this means to ignore newlines until "baseurl:"
                      A jekyll static site meant for generating quick page
                      layouts to make landing pages in a quicker manner.

baseurl: "/meadow" # the subpath of your site, e.g. /blog
url: "http://friends-of-pheasant-branch.github.io/meadow" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
permalink: pretty
collections:
  tables:
    output: true
  tabs:
    output: true
